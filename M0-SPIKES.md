# M0 Spike 执行清单
S1 API fixture（本地 mock + schema 解析）
S2 dsh CLI 行为（--ignore-scripts、路径 A/B 字节连续性、SRI 篡改检测）
S3 激活时序（PRE_DISABLE 有效性、dsh plugin add 是否改写 patch、HMR）
S4 会话 token 请求矩阵（curl + Chromium）
S5 journal crash-point（纯 Node 原型 + 故障注入）
S6 Desktop-shaped adapter（EffectPorts 最小实现 + contract tests）
S7 汇总 DECISIONS.md
