{
  "name": "cordis-mp",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.10.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "pnpm -r test",
    "smoke": "node scripts/host-smoke.mjs",
    "api:probe": "node scripts/cordis-run-contract-probe.mjs",
    "pack:check": "node scripts/npm-pack-check.mjs",
    "release:public-check": "node scripts/public-release-check.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
