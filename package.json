{
  "name": "cordis-mp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "pnpm -r test",
    "smoke": "node scripts/host-smoke.mjs",
    "api:probe": "node scripts/cordis-run-contract-probe.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
